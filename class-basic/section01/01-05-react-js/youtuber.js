const 채널페이지 = (props) => {
    return (
        <div>
            <div>채널에 오신 것을 환영합니다</div>
            <span>{props.rrr}</span>

            <button onClick={props.zzz}>구독하기</button>
        </div>
    )
}